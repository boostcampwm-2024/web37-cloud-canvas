export interface GridPosition {
    col: number;
    row: number;
}

export interface CoordPosition {
    x: number;
    y: number;
}

export interface GridSize {
    cols: number;
    rows: number;
    depth?: number;
}
